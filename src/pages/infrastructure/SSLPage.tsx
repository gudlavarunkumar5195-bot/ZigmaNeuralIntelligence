import { UnavailableAnalysis } from "../../components/ui/UnavailableAnalysis";
export function SSLPage() { return <UnavailableAnalysis title="SSL / HTTPS" dependency="A selected-scan certificate result endpoint" />; }

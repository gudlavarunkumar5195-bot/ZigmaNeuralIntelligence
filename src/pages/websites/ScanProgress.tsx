import { useEffect, useState } from "react";
import { useParams } from "react-router";
import { Activity, CheckCircle2, Clock3, XCircle } from "lucide-react";
import { apiGetScan, ApiCallError, IntegrationRequired } from "../../services/api";
import { ErrorState, IntegrationRequiredState, LoadingState } from "../../components/ui/DataState";

interface ScanRecord { id: string; status: string; started_at: string | null; completed_at: string | null; error: string | null; modules: string[]; scores: Array<{ category: string; score: number | null; status: string }> }
export function ScanProgress() { const { id = "" } = useParams(); const [scan, setScan] = useState<ScanRecord | null>(null); const [error, setError] = useState<Error | null>(null); const load = async () => { try { const result = await apiGetScan(id); if (result.error) throw new ApiCallError(0, result.error.code, result.error.message); setScan(result.data as ScanRecord); } catch (cause) { setError(cause as Error); } }; useEffect(() => { void load(); }, [id]); if (!scan && !error) return <LoadingState label="Loading scan execution…" />; if (error instanceof IntegrationRequired) return <IntegrationRequiredState feature="Scan progress" />; if (error) return <ErrorState title="Unable to load scan execution" message={error.message} onRetry={load} />; const icon = scan?.status === "completed" ? <CheckCircle2 className="text-emerald-600" /> : scan?.status === "failed" ? <XCircle className="text-red-600" /> : <Clock3 className="text-blue-600" />; return <div className="mx-auto max-w-4xl p-5 md:p-10"><div className="flex items-center gap-3 border-b border-slate-200 pb-6">{icon}<div><p className="font-mono text-[11px] uppercase tracking-[.18em] text-blue-700">Live execution</p><h2 className="mt-1 text-2xl font-800 text-slate-950">Scan {scan?.status}</h2></div></div>{scan?.error && <p className="mt-6 rounded bg-red-50 p-3 text-sm text-red-800">{scan.error}</p>}<section className="mt-8"><h3 className="flex items-center gap-2 text-sm font-800 text-slate-900"><Activity size={16} />Measured dimensions</h3>{scan?.scores.length ? <div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3">{scan.scores.map((score) => <div key={score.category} className="border-b border-slate-200 py-3"><p className="text-xs font-600 text-slate-500">{score.category}</p><strong className="mt-1 text-2xl text-slate-900">{score.score ?? "—"}</strong><p className="text-[11px] text-slate-400">{score.status}</p></div>)}</div> : <p className="mt-4 text-sm text-slate-500">No measured scores are available for this scan yet.</p>}</section></div>; }

#!/usr/bin/env bash
set -euo pipefail
pnpm --dir server install --frozen-lockfile
pnpm run build
[[ -d "dist" ]] || { echo "Expected build output at dist" >&2; exit 1; }
figma make deploy --build-dir dist
